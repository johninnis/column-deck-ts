{
  "name": "@innis/column-deck",
  "version": "0.3.0",
  "publish": {
    "exclude": ["tests/", "examples/", "deno.lock", ".github/"]
  },
  "minimumDependencyAge": {
    "age": "PT24H",
    "exclude": ["jsr:@innis/*"]
  },
  "license": "MIT",
  "description": "Multi-column UI framework: lifecycle, registry, mount/teardown, drag-to-reorder, keyboard shortcuts, mobile back-stack, sessionStorage-backed persistence. Domain-agnostic — every column is a black box.",
  "exports": "./mod.ts",
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "lib": [
      "deno.ns",
      "dom",
      "dom.iterable",
      "dom.asynciterable",
      "esnext"
    ]
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1",
    "@std/path": "jsr:@std/path@^1",
    "deno-dom": "https://deno.land/x/deno_dom@v0.1.48/deno-dom-wasm.ts"
  },
  "tasks": {
    "check": "deno check mod.ts src/ tests/ examples/",
    "example:minimal-host": "deno bundle --platform browser --outdir examples/minimal-host/dist examples/minimal-host/main.ts && deno run --allow-net --allow-read jsr:@std/http@^1/file-server examples/minimal-host --port 8088",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "test": "deno test -A",
    "coverage": "rm -rf cov_profile && deno test -A --coverage=cov_profile && deno coverage cov_profile --lcov --output=cov_profile/lcov.info && deno coverage cov_profile && deno run --allow-read jsr:@innis/coding-standards@^0.1.0/check-coverage",
    "exports-tested": "deno run --allow-read --allow-run jsr:@innis/coding-standards@^0.1.0/check-exports-tested",
    "docs": "deno run --allow-read --allow-run jsr:@innis/coding-standards@^0.1.0/check-docs",
    "publish:dry": "deno publish --dry-run",
    "ci": "deno task fmt:check && deno task lint && deno task check && deno task coverage && deno task exports-tested && deno task docs"
  },
  "lint": {
    "plugins": [
      "jsr:@innis/coding-standards@^0.1.0/lint-plugin"
    ],
    "rules": {
      "include": [
        "no-console",
        "ban-untagged-todo",
        "explicit-function-return-type",
        "no-non-null-assertion"
      ],
      "exclude": [
        "require-await"
      ]
    },
    "exclude": [
      "cov_profile/"
    ]
  },
  "fmt": {
    "semiColons": false,
    "lineWidth": 120,
    "exclude": [
      "**/*.md",
      "cov_profile/"
    ]
  }
}
